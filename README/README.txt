_ il faut avoir node.js et npm installer sur son desktop
_ cloner le git repository sur sa machine
_ Dans le terminal écrire la commande "npm run start" pour démarrer l'application

Remarque : si vous rencontrer une erreur du genre electron, node ou bien npm n'est pas bien configurer après installation:
    _ écrire cette commande qu'une seul fois dans le terminal "npm i -g electron" comme sur la photo electron bug fix.png
    

